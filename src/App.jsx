import { DriversList } from './drivers/DriversList';
import { Header } from './header/Header';

export const App = () => {
    return (
        <div>
            <Header />
            <DriversList />
        </div>
    );
};
